import { ClipboardList } from "lucide-react";
import { PlaceholderPage } from "../_placeholder";

export default function QueueManagementPage() {
  return (
    <PlaceholderPage
      title="Queue Management"
      subtitle="Monitor and manage the patient queue"
      icon={ClipboardList}
    />
  );
}
