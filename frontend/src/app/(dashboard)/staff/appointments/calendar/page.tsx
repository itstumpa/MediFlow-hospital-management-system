"use client";

import { Calendar } from "lucide-react";
import { PlaceholderPage } from "../../_placeholder";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Appointment Calendar"
      subtitle="View and manage appointments on the calendar"
      icon={Calendar}
    />
  );
}
