import { User } from "lucide-react";
import { PlaceholderPage } from "../../_placeholder";

export default function WalkInPage() {
  return (
    <PlaceholderPage
      title="Walk-in Registration"
      subtitle="Register walk-in patients without appointments"
      icon={User}
    />
  );
}
